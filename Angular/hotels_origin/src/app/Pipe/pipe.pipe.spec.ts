/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PipePipe } from './pipe.pipe';

describe('Pipe: Pipee', () => {
  it('create an instance', () => {
    let pipe = new PipePipe();
    expect(pipe).toBeTruthy();
  });
});
